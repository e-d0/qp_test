extends layout.pug

block content
    .jumbotron.mt-5
        .container#container
            form#form.form.col-md-12.col-lg-6
                h3.col Добавить пользователя
                .form-group.col
                    label(for='name') Имя пользователя
                    input#name.form_input.form-control(type='text', placeholder='Введите имя пользователя', required,  aria-describedby='passwordHelpInline')
                    small#namedHelpInline.text-muted
                        | Обязательно к заполнению.
                .form-group.col
                    label(for='password') Пароль
                    input#password.form_input.form-control(type='text', placeholder='пароль', required,  aria-describedby='passwordHelpInline')
                    small#passwordHelpInline.text-muted
                        | Обязательно к заполнению.
                .form-group.col
                    label(for='email') Электронная почта
                    input#email.form_input.form-control(type='email', placeholder='example@domen.some', required,  aria-describedby='emailHelpInline')
                    small#emaildHelpInline.text-muted
                        | адрес должен быть вида example@domen.some. Обязательно к заполнению.
                .form-group.col
                    label(for='age') Возраст пользователя
                    input#age.col-md-4.form_input.form-control(type='number', placeholder='Возраст', required, aria-describedby='ageHelpInline', pattern="[+]?[0-9]")
                    small#agedHelpInline.text-muted
                        | возраст должен быть больше чем 0. Обязательно к заполнению.
                button#send.ml-3.mb-3.btn.btn-success(type='submit') Добавить пользователя
                include message.pug

block append body
    script
        include register.js